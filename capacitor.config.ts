import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.segundocerebro.app',
  appName: 'Segundo Cérebro',
  webDir: 'dist'
};

export default config;
